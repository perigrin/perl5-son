# ABOUTME: CFG loop header node with entry and backedge control inputs.
# ABOUTME: Phi nodes at a Loop select between initial and loop-carried values.

use v5.42.0;
use feature 'class';
no warnings 'experimental::class';

class SoN::IR::Node::Loop :isa(SoN::IR::Node) {
}

1;
