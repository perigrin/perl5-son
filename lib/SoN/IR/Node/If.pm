# ABOUTME: CFG conditional branch node.
# ABOUTME: Takes a condition input; produces two control outputs via Proj nodes.

use v5.42.0;
use feature 'class';
no warnings 'experimental::class';

class SoN::IR::Node::If :isa(SoN::IR::Node) {
}

1;
